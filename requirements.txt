# Vercel's production build actually installs from pyproject.toml directly
# (via uv) - this file is for `vercel dev`/local Vercel-flavored dev per
# Vercel's FastAPI framework docs. Keep it in sync with pyproject.toml's
# [project.dependencies] regardless (runtime deps only, no dev/test tooling;
# neither path runs pytest/ruff).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
libsql-client>=0.3
